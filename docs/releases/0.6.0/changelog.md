# Miyu 0.6.0 完整更新记录


## 重要更新
- WebUI 的回复区重做了「过程」的画法。连续思考、调工具的那一串，以前是一堆宽度不一、底色各异的小标签，调十次工具就是十块彩色方块堆在一起；现在串成一条时间线：一根细线穿过图标列，图标就是节点，思考是一段淡色文字，工具是一行名字加等宽主语、右侧耗时。成功不打勾不上色，只有失败那一行是红的。新出一行时线会平滑长过去，不再一跳一跳；模型吐得快时几行会错开出场。点一行工具展开的是一个面板：参数、空一行、返回，没有小标签套小标签。
- 设置里新增「过程自动收起」（默认开，跨设备同步）：工具连续调用完成后，前面那串思考和工具收成一行 `Worked for 5.4 s · 3 tools · 1 thought`，想看再点开；正在运行时不会出现这一行。关掉就一直摊开。这个耗时现在会落库，刷新、换设备回看都还在，每次调用的执行起止也一并记下。
- AI 的回复不再套气泡，正文直接铺在页面上，靠头像行认归属；你的消息改用原先 AI 气泡的那个中性底色，不再是大面积主色。正文里的代码块、行内代码、表格随之重新定了底色，晨光主题下不再和米色底混成一片；你消息里的代码块也不再是一块深蓝。
- 消息上的时间去掉了：你消息旁的时间、AI 名字旁的时间（和回复完成后的「刚刚」）都不再显示。
- 回复还在跑的时候再发一条，不再进输入框上方的托盘，而是直接出现在对话末尾，就是它轮到时会出现的位置，左边一枚「排队中」小签和撤下按钮。轮到它时小签消失，变成一条正常的消息。
- 思考内容收着的时候，「已思考」那一行右侧会显示思考的尾巴：正在想就跟着滚，想完了也留着；展开才让位。「展开思考内容」因此默认改为关。

- 她写的网页在右边预览里能动了。以前 artifact 面板里的 HTML 是关在一个密封箱子里看的，箱子连电都没通——网页里的 JavaScript 一行都不跑，于是图表一片空白、按钮点了没反应、标签页切不动，只有纯文字和纯色块显示得出来。现在箱子通了电：脚本能跑、图能画、按钮能点。箱子照样是密封的，而且封得比通电前更明确——网页读不到你的登录凭证、读不到浏览器里存的东西、看不到 Miyu 主界面的内容，也没法把数据发去外部网站、开新窗口传出去或者把页面跳走。这几条都是实测逐条打过的。代价是网页里存不住状态，刷新一次就回到初始样子。
- 内置了 ECharts（6.1.0），她画图表不用再手搓 SVG。因为预览箱子是断网的，网上的图表库下载不到，所以直接打包进来了；同样内置的 KaTeX 和 Prism 现在预览里也能用。工具说明里写清了有哪几个库、怎么引、外网是断的，免得她写一串 CDN 链接然后画出一片空白。压缩存放，程序只大了 388 KB。
- 终端 REPL 改成全屏界面（默认开启，`MIYU_TUI=0` 可回退 inline）。回复、思考、工具调用铺成一条时间线：一根细线穿过图标列，思考是「已思考 · 词元 · 秒数」一行，工具是「名字 · 秒数 · 命令/路径」一行；正在跑的那一步左边转着点阵。回合结束后整段收成 `Worked for 12s · 3 tools · 2 thoughts` 一行，点开还是那条时间线，再点某一步是它的完整输出：编辑是 diff、命令是完整命令加输出、思考是全文。跑着的命令在抬头下面露最新四行输出，点开能看到流式的完整输出；编辑一落地 diff 就能点开看，不用等回合结束。鼠标滚轮回翻、拖选复制、点链接打开、`/history` 浮层都有。
- shellhook（在 shell 里直接对她说话）和单次 `miyu "…"` 也换成同一套时间线，只是没处点开：每一步跑完就落在屏幕上，命令那一行底下直接印几行输出（超出的换成「已省略较早输出」），编辑直接印 diff，报错的那一行打叉标红并印出命令输出；提问和回答也排进时间线里。
- 子代理点开是一个浮层：里面是它自己的时间线——交给它的差事、每一步思考和工具（含「准备xx」和正在跑的那一步）、它说的话按先后排，说过话之后前面那段收成一行；后台子代理同样能点开看，标题上有实时的词元数。后台任务完成时不再把开着的浮层抖一下。

## 修复
- `.svg` 文件在预览面板里彻底打不开的问题修了。以前它连「预览」和「查看源码」两个按钮都不给，面板里只剩一坨不换行的原始字节。现在直接显示图，能缩放能平移，也能切过去看源码、复制、下载。
- `.csv` / `.tsv` 现在画成表格，不再是一片用逗号隔开的原始文字。太长的表只画前 2000 行，剩下的提示去看源码或下载。
- 预览面板里「查看源码」终于有语法配色了。聊天区的代码块一直是有颜色的，只有这里是一片纯白——那段代码压根没接上高亮组件。
- 知识库重建语义索引点了不再白点。上一趟重建还在跑的时候提出的重建请求会**原地蒸发**——而一趟重建在开跑那一刻就把文件清单定死了，所以那之后传进去的文件谁也不会再管，锁一清面板又显示「空闲」，界面上一点痕迹都没有（拖进一整份 wiki 再点重建、几千个文件一个都没索引，就是这条）。现在改成排队，前一趟跑完自动接着建。
- 知识库重建失败不再无声无息。以前子进程的输出全被丢掉且没人等它，200 毫秒内崩掉和「跑完了」长得一模一样。现在失败原因显示在卡片上，完整输出落在 `<知识库目录>/embedding-reindex.log`，`miyu embed status` 也会报；重建进程半路没了也不用再等满一小时才认。
- 知识库面板不再自相矛盾地一边说「未配置模型」一边说「嵌入已配置」。用内置的本地嵌入模型时，概览卡读的是只有远程后端才填的那两个字段，所以永远显示未配置。
- 知识库的目录和库这一级现在有勾选框了，可以整个目录一次选中再批量删，不用展开后一个一个点。
- WebUI 的正文渲染不再把 `check_os_info`、`bilibili_live_stream` 这类名字里的下划线当成斜体标记。按 CommonMark 的规则，下划线两头紧挨着字母数字时不是强调；星号不受影响。
- WebUI 里工具芯片的文字过长时不再画到圆角背景外面，「已思考」那枚也不会被挤成「已…」。批量操作条在窄栏里不再散成三行。
- WebUI 里自己发的附件，图标会按类型区分了（视频、音频、PDF、压缩包、Markdown、代码各有各的图标），不再全是一个文件图标。
- 知识库的两个数据库改用 WAL，面板刷新和后台重建不再互相挡。
- 让她把 QQ 里的图存成表情包，以前经常直接失败、只回一句「不是严格 schema」。根因是识图模型十有八九会把 JSON 裹进 ```json 围栏或先说一句「好的，分析如下」，而这一处是全仓唯一没做围栏兜底的解析（另外六处同类调用都做了）；提示词里的 schema 示例本身就是围栏写法，等于在教它这么答。现在能收下围栏和开场白，模型多吐一个字段也不再让整次入库失败，真失败时报错会写清是围栏、少字段还是它在拒答。
- `/wipe` 不再删除自动生成的技能。技能和脚本是磁盘上的文件，不是她记住的东西；要连技能一起清，用 `miyu memory reset --include-skills` 明确要。
- 知识库不再把正常资料挡在门外。以前的检查是拿「skill / memory / config / 记忆 / 配置」这几个词去扫**整篇正文**，于是一篇讲内存管理的文档、任何出现过 config 的教程、任何带「配置」二字的中文资料统统传不进去（WebUI 手动上传和让她用工具写入都会被挡）。现在只看落点路径像不像 Miyu 自己的技能/人格/配置目录，以及文件是不是一份带标记的技能定义，正文里出现什么词一概不管。同时修了 macOS 上第一次往新知识库写文件必报「逃出目录」的问题。
- WebUI 的表情包面板改成瀑布流。以前一张瘦高图会把整行的行高顶起来，同行的方图只填得满三分之一格；现在每张按自己的比例占位，互不牵连，从左到右的顺序不变（搜索命中的前几张仍在最前）。
- 手动压缩（`miyu compact`、`miyu session compact`）不再干等着不吭声：摘要现在边生成边以暗色实时打出来，和自动压缩一个观感。以前这条路把压缩事件全丢了，几十秒里一个字都没有，末了才甩一句「已压缩」。管道里不上色但正文照出。
- 终端里聊到一半不再反复冒出「上下文渐大；将先机械折叠旧工具记录，随后才会自动压缩历史」。这句提示承诺的「机械折叠」其实一个字节都折不到（它折的那份数据从来只装少量精选小结，工具输出走的是另一条通路、且早在落盘时就截过一遍了），而它「一个会话只提示一次」的约定在后台服务形态下每轮都会重置，于是变成每说一句刷一遍。现在上下文水位只剩「压缩」与「强制压缩」两档，到点直接压缩；相应地移除了 `compact_soft_ratio`、`compact_snip_ratio`、`prune_stale_tool_reports`、`cold_prune_after_minutes` 四项配置（配置文件里的旧字段会被忽略，不用改）。
- 终端（kitty）里显示图片或公式之后，滚上去看历史时新输出不再把图片切片一条条复制到正文里；不滚上去时图片底部也不再单独留一条。根因是 kitty 受限区滚动不搬历史区的图片引用，现在正文里有图之后改用整屏滚动腾地方，纯文字会话不受影响。
- 用 opencode Zen 时，请求现在会带上 opencode 客户端的识别头。Zen 服务端按这些头分桶，一个都不带的请求会被当成匿名客户端对待（社区里第三方客户端「key 明明有效却狂吐 429」就是这么来的）。头的取值取自本机 opencode CLI 的实测抓包，只发给 Zen 端点，其它供应商一个都不带。
- 私聊里她一边干活一边说的话，现在每动一次工具就先发出来，不再全攒到回合末尾一次性甩给你。以前只有「再次请求模型」时才会把上一段话发走，而用 Claude Code / Codex / Antigravity 这类中转供应商时整轮只请求一次模型（工具循环在对面跑），中间说的话就全堵在最后——本机日志实测六天 42 次私聊回合、2570 次工具调用，中间消息统共只发出过 1 条，还是端点切换撞出来的。群聊沿用原有开关（默认关）。改权限、改主动判断名单这类由宿主代发结果的工具不受影响，仍旧只发宿主那句权威答复，不会先漏一句她的猜测。
- 设置里点开一个 QQ 插件后，切到内容长的标签页时，顶上那排标签会被正文挤扁成一条细线，八个标签全看不见、只能盲点。抽屉是列向弹性布局，而标签条自己带的裁剪属性让它的最小高度变成 0，内容越长被摊掉的高度越多（本机实测「主动回复判断」那页把标签条从 39px 压到 14px，按钮被裁掉 24px）。现在标签条锁死高度，内容再长也只滚正文。
- 手机上用 Safari 打开 WebUI，点输入框会把整个页面顶上去、只剩一片黑，Chrome 和 Firefox 都正常。根因是页面外壳的高度写死成整屏高，而 iOS 的软键盘不改这个高度（页面里那句 `interactive-widget=resizes-content` 只有 Chromium 认），于是外壳比看得见的区域高出一个键盘，浏览器为了把输入框顶进视野就滚到了下面那片没人画的空白上。现在外壳跟着可视区域一起缩，键盘弹出时只有输入框上移；键盘弹出瞬间浏览器已经滚出去的那段偏移也会收回来（捏合放大时不抢方向盘）。
- 长文转图片里的表格不再乱折行。以前列宽是死板等分——一张五列表格里「排名」「笔数」这种只装一个字符的列，和九位 QQ 号一样宽 192px，号码正文只剩 164px 放不下，就被从中间劈成两行（`59611392` / `0`），长一点的人名也被拆得七零八落。现在先量每列内容的实际宽度再分配：装得下就按需要给，装不下只削最宽的那一列，窄列不受牵连；实测同一张赞助榜图高从 2009px 降到 1327px。
- 长文转图片里的链接和图片不再只剩标题。以前 `[标题](链接)` 画出来只有标题、地址整个消失，图里点不了也复制不了，读者连它指向哪都无从知道；`![](地址)` 更是什么都不剩。现在会在后面补一段 ` (地址)`，自动链接（标题本身就是网址）不会写两遍，表格单元格里同样生效。顺带把 paper 主题的链接色从暖棕换成深青蓝——字体资产只带 Regular 字重，**加粗**是靠变色呈现的，原来的链接色和加粗撞在同一个色相上，两者看着一模一样。
- 目标任务（`/goal`）自动续轮跑着的时候，在 WebUI 里再发消息不再弹「这条没发出去：会话刚开始新的一轮」。服务端其实认出了那是续轮，却拿错了身份去排队，被自己的校验挡了回来；现在消息会排进正在跑的那一轮，到下一个工具边界被取走，排队托盘里也能删掉。终端一直是好的，只有 WebUI 中招。
- WebUI 控制台里刷新页面不再回到聊天页：现在停在哪个面板（含设置页的哪个子页）就回到哪里，地址栏的 `#console/…` 也可以直接分享给别人。
- 撤回 QQ 消息时日志会写清撤的是谁的消息了：昵称、QQ 号、原文前 80 个字、理由和会话，成功失败都有；给模型的工具结果里也多了发送者昵称。以前那行日志只有模型自己传的参数，事后完全查不出撤掉的是哪条。
- 在设置里改供应商的「配置 ID」后，数据统计里的历史用量会一起改名，不再分裂成新旧两行，改名前的费用估算也跟着恢复（以前旧 ID 在配置里查不到，取不到接口地址，消费列只显示「—」）。终端的配置菜单里改 ID 同样生效。这次更新之前已经改过名的旧记录保持原样。
- 设置页在宽屏或最大化窗口下居中显示了，不再贴着左边、右边留一大片空白。
- 从不同的地址进 WebUI（比如在电脑上用 127.0.0.1、在手机上用局域网 IP），主题不再各是各的。浏览器的本地存储是按网址隔离的，换个地址进来等于换了一份空白设置。现在主题、配色、正文字号、思考块和工具块的默认展开状态存在 Miyu 这边，哪台设备哪个地址进来都是同一套；侧栏折叠、分栏比例这类跟屏幕尺寸绑在一起的仍然各设备各记各的。
- WebUI 的代码块不再画一圈框，层次交给底色。自己发的消息里的代码块底色也改成跟着气泡走——以前它借用正文那套代码底色，亮色主题下气泡是深色实心、代码块却是页面那张浅色的纸，等于在深气泡上贴了张白纸。
- 终端和 WebUI 里，`标题 (网址)` 这种独占一行的参考资料写法，现在标题也是链接色，点标题和点网址都能打开（终端走 OSC 8 超链接，kitty、foot、wezterm、Konsole、GNOME 终端这些都支持）。终端里正文中间直接甩出来的网址以前是一段没有颜色的纯文字，现在也上色成链；句尾的句号、顿号仍旧不会被吃进地址里。
- WebUI 里的链接改回固定的蓝色（深浅主题各一支柔和的蓝），不再跟着主题的强调色走。用壁纸取色时强调色是从壁纸里抓的，于是「链接」在橄榄色壁纸下是橄榄色、在酒红壁纸下是酒红——而蓝色带下划线是所有人对「可点」的肌肉记忆，这点主题一致性不值得拿它去换。终端那侧本来就是固定蓝，两边现在一致。
- 指向本地路径的链接（`file://` 开头，比如她告诉你某个 MCP 服务器装在哪）以前整条原样漏成 Markdown 源码。现在终端里它是可点的超链接；WebUI 里点一下是**复制路径**——浏览器不允许网页跳到本地文件，做成真链接的话点了什么也不会发生。

- 压缩一个会话时，**其他会话不再全部瘫痪**。以前一次压缩会同时从两头卡住所有人：压缩在核心循环里独占执行，别的会话的消息只能排队干等；同时那把「管理锁」是全局的，于是排到了也照样被一句「Miyu 正忙」顶回来——哪怕你要说话的是完全不相干的另一个会话。现在压缩挪到后台跑，管理锁也按会话划分（改配置、换模型这类真正全局的操作仍然挡所有人）。
- 压缩不再动不动就超时白跑。超时线以前写死 90 秒，而慢一点的模型在长会话上光生成摘要就要好几分钟，必然被砍在半路、什么都没留下，还要再重试两轮。现在超时按摘要的输出预算算（满配约 5 分钟），并且第一次超时后不再用更慢的方式重试一遍。
- 压缩摘要不再越写越长。摘要里「逐条列出用户说过的话」那一节原本只在**已有摘要**的情况下才有条数上限，于是第一次压缩一个长会话时她会老老实实列上几百行，把输出预算撑爆、直接导致上一条说的超时。现在这个上限一直生效：最近 20 条逐字保留，更早的合并成几行。
- 手动压缩不再长时间一片空白。摘要前的「先打草稿再落笔」环节默认关掉了——草稿不会显示给你，写多久屏幕就空白多久，而实测它并不提升摘要质量。想要可以用 `MIYU_COMPACT_ANALYSIS=1` 打开。
- 压缩摘要那一轮的缓存命中不再记成 0。摘要请求是拿实况对话的前缀去问的，实测 96.6% 的输入都是缓存命中（26911 个 token 里命中 25984），但落库时归一化后的命中数被漏掉了，于是数据统计里这一轮永远显示 0 命中，压缩越多、整体命中率被拉得越低。
- 终端和 WebUI 上显示的上下文占用改用供应商报的真实数字了。以前一律是本地估算，而估算器按 OpenAI 的分词规则数，中文按「两个字一个 token」硬折——换个供应商就系统性偏，触发压缩的时机也跟着偏。现在每轮结束记下供应商自己报的用量，下次直接用它；刚压缩完等数据还没回来的那一小段仍旧走估算。
- 终端 REPL 回车后光标不再先飞到屏幕左下角再回输入框。真凶是 kitty 的 `cursor_trail`：它会给隐藏着的光标画拖尾，而回显把屏幕顶满时光标正好被推到左下角，等 daemon 接手回合的那一百多毫秒里输入框还没画回去。现在回显和输入框在同一帧里画完，光标从不在左下角落脚。
- 终端 REPL 粘贴修了两处：多行粘贴的占位符以前老显示「~1 行」、发出去还会把几行粘成一行（终端把换行送成回车符，Miyu 没认）；单行粘贴以前超过 150 字符就折成占位符，中文一句话就中招。现在按它在输入框里实际占的行数判定，占到三行以上才折叠。
- 在 shell 提示符里直接跟她说话（shellhook）、她反问你一个问题、这时终端被关掉或进程被杀之后，下一句她不再失忆。以前那一轮会永远卡在「进行中」、被历史悄悄跳过；现在断线即按中断收尾，下一轮照常看得到。回合内的提问也加了 30 分钟上限。
- QQ 里生图之后不再把同一句话发两遍。以前工具已经把「画好了」连图一起发出去，最终回复又原样再来一句，短句还躲过了近似判重；现在本回合工具发过的话最终回复再发会被拦下。

- 用 Claude Code / Codex / Antigravity 当供应商时，上下文压缩现在能记住模型碰过的文件。以前这三条线上模型用自家 Read/Edit/Write 读写的文件从不进压缩摘要的 `<read-files>` / `<modified-files>`，压后回灌也永远是空的；现在会记下来，压缩后最近改过的文件会重新灌回上下文（Antigravity 的 view_file / write_to_file、Codex 的文件改动同样认得）。
- Claude Code 供应商不再把上下文窗口透传给 claude 自己的压缩：压缩统一由 Miyu 做。之前那份透传其实从没生效过——Miyu 的压缩线总是先到，两套并存只剩歧义。
- 终端 REPL 里按上键找回带粘贴占位符的那句话时，输入框里还是 `[粘贴 1: ~40 行]` 这个占位符，而不是四十行裸文本把输入框撑满。以前历史里存的是展开后的全文；现在占位符连同它的内容一起进历史，退格整块删、再发出去照常展开，重启之后也一样。`[Image N]` 这类图片占位符同理，找回来还带着图。
- Safari 里她流式回复、气泡往下长的时候，页面底部不再上下鬼畜跳动。根因是页面主舞台上挂着的一条 CSS 容器查询：WebKit 在这种容器的后代被整块替换时会把聊天区的滚动位置同步归零，随后自动跟随又把这次归零当成你在往上翻而关掉——Chrome 和 Firefox 没这个行为。那条容器查询只服务宽屏时的常驻任务面板，现在改由脚本量宽度挂类名，顺带把「同一帧里多次滚动请求互相作废」和「程序滚动被当成用户滚动」两处也修了，代码高亮和链接卡片落地后视图也会跟上去。
- 在 shell 提示符里直接跟她说话（shellhook）、她反问你一个问题时，面板不再几秒后自己消失、回合被取消。真凶是挂断看门狗盯的是 stdin：shellhook 是用管道把命令行喂给她的，写端一退出 stdin 就报「挂断」，五秒后进程自杀。现在 stdin 是管道时改盯控制终端。
- 终端里她的回复不再被拆成一行一段（「问我的模型的话」「那还是不说，」「这」各占一行、中间空一行，连行内代码都被从中间劈开）。根因是有些供应商在每个正文分片上都附带一个空的 `reasoning_content: ""`，Miyu 把空串也当成一段推理的开始，于是每个分片前都插了一次「推理分段开始」，终端一收到这个信号就把正在打的正文行截断。现在空推理字段按「没有」处理（Chat、Anthropic、Responses、Claude Code 四条协议同修），终端侧也改成只有真要写思考摘要时才断行。
- 终端 REPL 里 `/models` 的结果行（「当前会话模型: …」「当前会话已恢复跟随全局激活模型池」）不再被画到输入框那一行上或者整个被盖掉。选择器和结果行是直接往终端打的，而输入区还挂在屏上、也不知道多了几行；现在先收起输入区、打完再按真实光标位置挂回去。
- 手机上 WebUI 的回车改成换行了，发送用右下角的按钮（或 Ctrl/Cmd+Enter）。软键盘上没有 Shift+Enter，以前回车即发送，多行消息根本打不出来。桌面端不变。
- 手机上数据统计页的环形图不再挤在表格左边把表格压成一条：窄屏下改成环形图在上、表格整行在下，断点和手机布局其它部分对齐（836px）。
- 「回到底部」按钮不再压在后台任务条上。它离底部的距离是按输入区高度写死的一个数，后台任务条出现或增行、软键盘顶起视口时输入区变高，那个数却没人更新，按钮就留在原地盖住任务条，手机上一点就误触。现在直接盯输入区的尺寸变化，谁改都跟上。
- 打开页面、刷新、切换会话时，上下文条显示的窗口大小现在是**这个会话**当前模型的，不再是全局默认模型的。会话钉了模型池时，回合和压缩两条路都按那个池算，唯独快照接口漏了，于是要跑完一轮才被纠正；打开一次设置页也会把它改回全局值，同修。会话内换模型（仅本会话）后也立刻更新，不用再等跑完一轮。
- WebUI 整段重建时间线（后台任务完成、目标续轮这类由 daemon 自己发起的回合落盘时）不再把「自动跟随到底部」关掉：重建会让浏览器把滚动位置钳到顶再跳回底，中间那次归零以前被当成你在往上翻。
- 记账面板「本月支出」底下那行笔数以前数的是当月**所有**账目，一记收入或转账，支出旁边就会出现一个比它自己大的数。现在只数支出笔。
- WebUI 里删掉侧栏最后一个会话，不再一下冒出两个新会话。顶替的空会话原来由每个页面自己新建，桌面和手机（或两个标签页）同时开着那个会话时就各建一个；现在改由服务端建一个、各页面共用。
- 终端 REPL 里 `/models` 换完模型，输入框下那行的模型名现在立刻跟着换，不再要等敲下一个键才刷新。

## 重要更新
- QQ 配置新增「睡眠时间」（TUI「腾讯 QQ」菜单与 WebUI 设置页都有）。填 `HH:MM-HH:MM`，可跨午夜，例如 `23:00-07:00`。时段内只有管理员（私聊/群聊）和私聊白名单（仅私聊）的消息还会叫醒她，其余一切消息——包括群里 @她、关键词触发、文件上传、撤回、戳一戳——都当没看见，不进历史也不概率插话。留空即关闭。
- 压缩之后她不再对着一份只剩概要的记忆干活：压缩会把折叠区里最近读过、改过的几个文件**从磁盘上重新读一遍**放回上下文（默认 5 个、单个 4000 token、总共 24000 token，窗口小的时候自动缩水），所以刚压完你问「那个文件第 10 行是什么」，她直接答得出来，不用再调一次工具。太大或超出预算的文件只留路径，她需要时自己读回来。
- 压缩掉的对话不再彻底消失：折叠掉的那些轮次会连同被取代的上一份摘要一起写成一份 Markdown 存到 `state/compact/<会话>/`，路径直接告诉她。摘要里没写到的细节，她可以自己把原文读回来看。最近 5 份都留着。
- 压缩摘要本身也重写了一版：多了「所有用户消息逐条」「错误与纠正」「当前工作」三节，「下一步」现在必须绑住你最后一次明确的要求，不再自己发挥。输出预算也放宽了一倍，长会话的摘要不会再写到一半被截断。
- 知识库重建语义索引有进度了：百分比、已处理 / 总数、当前正在处理哪个文件，外加一根进度条；跑完和失败都有明确提示。
- WebUI 里自己发出去的消息现在会渲染代码块和行内代码，链接也可以直接点。刻意**不做**完整 markdown——把你随手打的 `*星号*` 变成斜体、`# 井号` 变成标题，等于把你原样打进去的字改掉了，而她收到的仍是原文，两边会对不上。
- WebUI 里她说的话中间的网址现在可以直接点了。以前只有写成 `[文字](网址)` 才是链接，直接甩出来的网址是一段点不动的纯文字。行内代码和代码块里的地址一个字节都不动。
- 独占一整行的链接会升级成带图卡片：标题、简介、站点图标、缩略图，点一下打开。一条消息最多三张，其余保持普通链接；抓不到信息就原样保留，正文不会变形。抓取和图片都由本机 daemon 代劳，你的浏览器不会直接连到那些站点上去。
- WebUI 里自己发给她的附件，点一下是**预览**而不是下载：文本和 Markdown 直接看正文，PDF 直接翻，视频和音频给播放器，图片放大看。下载挪到芯片右边那个箭头上。
- 知识库面板支持拖放上传了：一次拖多个文件、或者整个文件夹拖进来都行，每个文件单独显示成功 / 已存在 / 被拒（带原因）/ 跳过（不是文本、超过大小）。
- `/reset-memory` 改成只清**本次会话**记下的记忆，并会告诉你清掉了几条；要清当前人格的全部长期记忆，用新命令 `/reset-all-memory`（QQ、终端、REPL、WebUI 都有）。注意：这次更新之前存下的旧记忆没有会话标记，只能用 `/reset-all-memory` 清。
- 她用网络搜索或读网页之后，会在回答末尾按 `标题 (网址)` 逐行列出用到的来源。
- 新增赞助记账。在 QQ 里可以让她记一笔赞助（谁、多少钱、备注），支持人民币和美元——美元按记账当刻的实时汇率折成人民币并把汇率一起存下来，所以排行榜的名次不会随汇率来回晃。记账、改备注、删除限管理员，看榜和查询谁都可以。WebUI 控制台新增「赞助」面板：总额、人数、笔数、前三名领奖台、名次榜和明细表。
- Miyu 现在可以当别的软件的 AI 后端了。`miyu ask` 新增 `--output-format json|stream-json`（每行一个 JSON 事件，含最终正文、用量、模型、耗时）、`--model` / `--context-window` / `--system-prompt` / `--append-system-prompt` / `--no-memory` / `--tools` / `--no-tools` / `--image` / `--cwd` / `--timeout` / `--stdin`，这些只对本次调用生效、不改配置；退出码区分用法错误(2)、会话不存在(3)、超时(124)、取消(130)。
- 新增 `miyu stdio` 长驻协议模式：宿主起一个进程常驻，stdin 一行一请求、stdout 一行一事件，多会话多回合可并发，支持她提问后由宿主作答、中途取消。协议见 `docs/cli-backend.md`。
- 新增 `miyu session` 会话管理面：list / new / show / delete / rename / clear / pop / compact / models / workspace，`--json` 直出；`--session 名 --create` 不存在则新建；`miyu reset` / `miyu pop` 也认 `--session`。
- 单回合正文上限从 2 万字符提到 20 万字符，方便用 `--stdin` 喂整篇文档。
- 新增 `miyu compact`：立即压缩终端集成会话的上下文，用法与 `miyu reset` / `miyu pop` 一致；`--session 名字|编号|id` 可指定别的会话。程序驱动的宿主仍用 `miyu session compact <目标>`。
- 「供应商和模型」菜单里可以手填模型名了：按 `n` 输入名字即可，供应商 `/models` 目录里没有的内测模型也能用上。手填的模型钉在列表最上面、标着「(自定义)」，激活、编辑、搜索、组织分栏跟拉取来的模型完全一样，取消激活也不会消失；按 `d` 删除（`u` 可撤销）。设置页的「手动添加」现在也会同步显示到这个菜单里。为了不误删，模型列的 `d` 只删自定义模型，删供应商请到左边两列。
- 可以给她发 PDF 了。QQ 里发的 PDF 文件、终端里粘的 PDF 路径（Ctrl+V 会出 `[PDF N]` 占位符）、WebUI 上传的 PDF 走同一条路：当前模型认 PDF 时（Claude、Gemini、GPT 系大多认）文件本体原样送到她眼前，而不是像以前那样被当成「不支持的二进制格式」挡回去。模型不认 PDF 时会把文件路径告诉她，她还可以用原生文件工具或命令行去处理。单份上限 20MB。
- WebUI 输入框为空时那句灰字（原来写死的「给 Miyu 发消息」）现在可以改了：人格设置 → 看板 → 「输入框提示」。留空则按人格名生成「给 <名字> 发消息」——顺带修了以前改完人格名、输入框还留着旧名字的问题。
- 记账来了。跟她说「中午吃饭花了 3000 日元」就记下了，WebUI 控制台里有完整看板（流水 / 账户 / 分类 / 预算 / 数据五个视图）。几个要点：
  - **外币自动换算**。账本设一个目标币种（比如人民币），记外币时按当天汇率折算过去，汇率连同来源一起冻结在这笔账上、之后永不重算。取不到汇率也照记，标成「待换算」并明确说它没进合计——统计宁可少算，也不拿猜出来的汇率把数字凑齐。
  - **月度预算**。可以给整本账或某个分类设额度，用到八成她记账时会提一句，超了会直接报出来。子分类的花销算进上级。
  - **多账本**。工作和生活可以分开记，各有各的币种、账户、分类和预算。
  - **账户是可选的**。它回答的是「这笔钱从哪付的」；只想记花了多少的话一个都不用建，记账表单里那一栏会自己消失。
  - **CSV 导入导出**。导出带汇率快照，能原样导回来；导入按内容去重，同一份文件导两次不会变成两份。
  - 防手滑：几分钟内记了内容完全相同的一笔会被拦下来问一句；删除是软删除，账目留在库里。
  - 账本存在独立的 `data/ledger/ledger.db`，不和聊天记录挤在一个库里，导出/迁移会带上它。只在终端和 WebUI 可用，QQ 侧不提供。
- 她在 QQ 里查你好感度关系时不再照着念后台数据。那个只在开启好感度功能、且只在 QQ 会话里才出现的查询工具，以前返回的是一份可以直接朗读的中文状态面板（「中立」「好友」这样的档位名 + 标签 + 备注），她拿到就复述成「当前好感度是中立，标签有……」。现在档位在内部改用英文代号，要说出口得先自己翻译一道，而翻译天然逼她改写成自己的话；「这是你自己的感觉，不是拿来照念的记录」这句护栏也写进了工具说明。返回体同时瘦身，去掉了显示名、写作要求和恒为空的字段，同一份档案实测 654 → 411 字符。WebUI 好感面板和日志里的档位仍旧显示中文，不受影响。
- WebUI 的代码块有语法高亮了：Rust、Python、JS/TS、Go、C/C++、SQL、shell、JSON/YAML/TOML、diff 等常见语言都认，认不出的语言原样显示纯文本。配色用的是 One Dark / One Light 那两套标准色，跟着你的主题走。上色只切分不改字——万一某门语言的规则吞了字或多了字，整块退回纯文本，宁可少一块颜色也不改你和她写的代码；超长的代码块也直接跳过，不卡页面。顺带把代码块做成了看得出边界的面板：底色跟着页面的纸色走、比气泡差开一档，靠颜色分层而不是画一圈框；补丁的删除行改回红色（原先是关键字那个紫色）。
- 多了一个 Reddit 检索工具（`reddit_search`）：可以搜某个版块里的帖子、读一整个帖子连同它的评论树、看某个人发过什么、按名字找版块。数据取自 Arctic Shift 这个公开的 Reddit 归档镜像（延迟只有几分钟），Reddit 官方 API 现在申请凭据要审核，所以没走那条路。因此有两件事它做不到，工具会直说而不是给你一份看着像样的假结果：一是没法全站搜关键词，关键词必须配一个版块或一个人；二是归档没有「热门」排序，你要「本月最热」时它会把这一个月切成几段各取一批再按赞数排，并在结果里注明这是采样不是完整排名。另外帖子发出后 36 小时内归档里的赞数还没回填，这段时间它会写「赞数未结算」而不是照着念一个 1。
- WebUI 的链接卡片出得更快：等待流式渲染稳定的时间从 0.7 秒缩到 0.25 秒，多条链接各自出卡不再等最慢的那条，缩略图和站点图标并行抓取。注意 bilibili 对不带 cookie 的抓取常常直接拒绝，那种情况是没有卡片而不是慢。
- 撤回消息的工具结果和日志会附上被撤消息的内容，图片、语音、视频、文件等媒体显示为 [图片]、[语音]、[文件 名字] 这样的占位符。
- 开发模式（`miyu dev`）瘦身，让模型少受无关提示词干扰。本机实测一轮的固定开销从约 3900 token 降到约 2950（−24%）：
  - **不再有记忆功能**。开发模式不再记日记、不再自动联想，也没有 `remember_fact` / `recall_memories` 了。以前它会把上一个开发会话的闲聊记录当成「近期发生的事情」灌进新会话——写代码用不上这个，反而把闲聊的语气带了回来。普通模式的记忆一切照旧；关掉之前存下的开发模式记忆还在库里，用 `/reset-all-memory`（选开发模式）随时清得掉。
  - **不再有技能**。开发模式没有创作技能的工具，却看得见一份技能清单——而且那份清单是**普通人格的**技能（抖音下载、显卡直通之类），对写代码毫无用处。需要给它技能的话，在 `config/dev-prompt.md` 里写一行目录路径，让它自己去读。
  - **按需加载工具只在用得上时才出现**。工具目录是「全部展开」模式时 `load_tools` 谁也不会去调，这种会话里不再挂着它；历史上真调用过的会话仍旧保留，不会撞上未知工具。
  - 逐出上下文检索（`search_evicted_context`）随记忆一起退场。注意：开发模式的 `miyu pop` 弹出去的对话不再进归档库，也就找不回来了。
  - 用 Claude Code 订阅当供应商时，桥给 claude 那边挂的 Miyu 工具从 11 件减到 6 件；`edit` 也交还给 Claude 自己的 Edit/Write——两者管的是同一件事，而中转这条线上 Miyu 的 `edit` 连修改前后的对比都显示不出来。
- 终端 footer 和 WebUI 回合结束的那行计量里现在显示她的输出速度：终端是 `361 tok/s · 26.0k/1M(2.6%) · Σ249.2k(C96%)`，窗口太窄时最先省掉的就是它；WebUI 是 `每秒 361 toks · 本轮 23k（C96%）…`。算的是每次模型请求从第一个字到最后一个字的墙钟时间，不含等首字和跑工具；用量是估算的、或者请求一口气整段返回没法计时的，就不显示。走 Claude Code / Codex / Antigravity 这类中转时对面自己跑工具的时间也算在里面，数字会偏低。刷新页面后历史回合不带这个数。
- 记忆整理（把近期日记蒸馏成长期知识点）换了一版规则，产出从「百科条目」变成「关于你和这台机器的事」。以前她会把技术问答的整段解答存成事实（本机真实库 123 条里六成是这种：Redox OS 是什么、fcitx5 主题怎么改），又长又占召回名额，还挤掉真正该记的。现在只存四类：具体人物的稳定事实与偏好、这个环境独有的配置与结论、带日期的实测结果与约定、她自己的认知；通用知识、教程、新闻和一次性解答明确不存，每条一句话不超过 120 字，会过期的写上日期。同一件事换个说法不再重复建条（词面和语义两道去重，近义的改写成对已有条目的更新，旧内容留在修订记录里）；召回排序纳入可信度和把握，一条事实被反复召回霸屏时会被按回同一起跑线。整理器默认改走标准档模型池；一条不合规的产出不再拖垮整批、整批连续失败四次后放弃而不是每五分钟重试到永远。同一批日记的对照：旧版存的是 Redox OS 百科和 Arch npm 拆包，新版存的是「本机没装 npm 用 bunx」「2026-09-09 实测 dsh 工具配置在哪」。
- 记账几处顺手的地方：
  - **分类不用先建了**。跟她说「电话费记 1379 日元，分类通讯」，账本里没有「通讯」也直接记下、顺手把分类建出来，并在结果里说一声是新建的——以前是甩一条「没有这个分类」的错误回来，她得再去建一次分类、再记一遍账，白花一整轮。改分类（update）同样如此。两处例外：名字同时像好几个现有分类时仍旧报错让你说清楚，那种时候新建只会让分类表更含混；名字长在另一棵树上（拿收入分类「工资」去记一笔支出）也报错，因为那多半是收支类型填错了，在支出下再建一个「工资」等于把真正的问题藏起来。筛选（list）不会建分类——建出来一条账目都挂不上。
  - **账户余额按账户自己的币种折算**。以前币种和账户对不上的账目会被整笔跳过：一个人民币账户上挂了五笔日元支出，余额纹丝不动地显示 0.00。现在按**记账当时**冻结在那笔账上的汇率折过去再扣（20366 日元的支出挂在人民币账户上就是 −889.87 元），跟汇率之后怎么变没关系。汇率还没取到的账目仍旧不进余额，但会明说有几笔没算进来。
  - **新账本自带一套账户**：现金、电子支付、交通卡、信用卡、银行卡，都跟着账本的目标币种。账户依旧是可选的，只想记花了多少就一个都不用填。已经在用账本的人不受影响：只有一个账户都没建过的账本才会补上这套，已经按自己的钱包配好账户（哪怕是外币的）的账本一个都不会被塞进去。
  - **定了预算的话，「结余」那张卡改成显示预算余额**（还剩多少、还剩百分之几）。只记支出不记收入的账本里，「收入 − 支出」永远是支出的负数镜像，那一格屏幕换不来任何信息。没设预算时仍旧显示本月净额。
- 修了一类会让会话「一发消息就报错、从此再也用不了」的隐患。某些模型（如 DeepSeek）要求：AI 每次调用工具，紧跟着必须有每个工具的返回；少一条就直接拒绝整条请求（`must be followed by tool messages responding to each 'tool_call_id'`），而这条坏消息会被存进历史，之后每次发消息都在同一处炸。跨供应商切换、上一轮被中断、历史回放边界这些情况都可能漏掉一条返回。现在发出去之前有一道兜底：缺哪条就补一条占位返回，会话照常往下走，同时在日志里留痕以便定位真正漏返回的地方。
- host-environment（告诉她当前主机、模型的那段）里去掉了「思考档位」。档位在对话中可以随时切换，把它写进系统提示词会让每次改档都掰断前缀缓存、白烧一轮的钱。现在改档不再影响缓存。
- **开发工作可以整包丢给子代理了**。她现在能开一个「开发模式」的子代理去写代码、改代码、排错、跑构建和测试：那个子代理拿的是和 `miyu dev` 会话一模一样的一套——开发提示词、极简的编码工具面（命令、后台任务、补丁编辑、搜索、todo、网页），不带记忆、技能、表情包那些东西，所以写代码时不会被闲聊语域带偏，也不会把编码过程写进她的记忆。它一开始就知道自己在哪个目录、这台机器是什么系统，不用先浪费一轮去问。人格提示词里也写明了：开发的活委派出去，她自己不下场动代码。顺带一提，子代理这件工具改名叫 `subagent`（原来叫 `task`），旧会话里的历史记录照常显示成「子代理」。
- 后台跑的子代理不再掉出会话的作用域。以前 `background=true` 的子代理一转到后台，会话的工作目录和沙盒限制就丢了：它跑命令时用的是 daemon 自己的目录，成员账号后台起的子代理还会跑在 Landlock 沙盒之外。现在后台子代理和前台的一样，工作区、沙盒、会话身份都在。
- 成员的知识库「重建语义索引」不再必然失败。以前成员点重建,后台起的子进程不认成员身份,跑去建的是默认库,而不是成员自己的 `home/<用户>/kb`;成员这份进度永远停在「启动中」,几百毫秒后就被判成「上次重建失败:reindex process exited without indexing」,而他的库其实一个块都没嵌上。现在子进程被告知该建哪一个库,重建落到成员自己的库;这种由看板触发的定向重建也只碰知识库,不再顺手重嵌记忆和表情包。
- 成员的 artifact(以及生成的图)现在落在自己家目录里，读得到也预览得出。以前成员读 `artifact:某文件` 会报「不在你的工作区，不允许读取」——因为 artifact 目录一直算在管理员名下，成员的沙盒够不着。现在成员的 artifact 落到 `home/<用户>/artifacts`，沙盒放行自己的这块地方（还有文档、图片目录只读）。
- artifact 预览能显示 PNG / JPEG / WebP / GIF 了。以前这些位图会被当成文本，在预览里显示成一屏乱码（`�PNG…`）；现在按图片渲染，能缩放平移，和 SVG 一样。
- 缓存命中率的显示口径改了：只有高于 99.9% 才显示成 100，99.1%–99.9% 保留一位小数（不再把 99.5% 抹成满分），99% 及以下取整。终端和网页端一致。
- 打开 artifact 预览时，左边的任务列表浮层不再盖住对话。
- 子代理的工具行不再显示成一块独立的灰色标签。收起时在标题行里单行滚动显示子代理当前在做什么（和「已思考」标题右侧的尾巴一样）；展开后能看到子代理自己的思考与工具流，和主智能体的过程区同款。
- 子代理的 vision（看图）修好了。以前子代理里让它看图，多数情况下它其实什么都没看到（图被寄存起来却没人取，它只拿到一个占位标记）；现在子代理里的看图一律走独立视觉模型转成文字，任何子代理模型都能用上。
- 后台子代理修好了两处：一是它跑在新线程上时丢了工作区/会话/沙盒身份，导致相对路径乱套、沙盒失效，用 Claude Code 当后端时还会「守护进程没连上（CONNECTION_CLOSED）」——现在这些身份会带过去；二是新增「给后台子代理发消息」的能力，主智能体可以在后台子代理运行途中追加一条指令（像给主会话排队消息那样），子代理下一步就会读到并调整任务目标。
- 后台子代理现在能看实时进度了。任务条里的后台子代理那一行可以点开，展开后是它自己的思考和工具流（和前台子代理工具行、主智能体过程区一样的渲染）；收起时那行右侧单行滚动显示它当前在做什么。多个后台子代理并行时各占一行、各自独立展开。
- 选了多个模型时终于会平均负载了。之前多模型池的轮询用的是一个全局游标，被分级池和判定/记忆/视觉/子代理这些辅助请求共用、各自推一格，结果主对话的轮询被打偏，看起来「一直在请求同一个模型」。现在每个池各有各的游标，独立轮换，互不干扰。
- 普通用户也能看到后台任务的状态行了。之前后台任务的事件没带归属会话，被事件过滤当成只发给管理员，所以普通用户那边的状态行整个不显示。现在带上了，普通用户能看到、点开、收起自己的后台任务。
- 普通用户用 Claude Code 等 CLI 当后端时，Miyu 的 MCP 工具桥能用了。之前 CLI 被沙盒关着，它起的桥进程连不上 daemon 的本地 socket，报 CONNECTION_CLOSED；现在放行了那个 socket 所在目录。桥转发的工具调用仍按这个普通用户的作用域执行，不越权。
- 子代理在网页端展开后，看到的是它自己的「思考 + 工具流」时间线（和主智能体的过程区一样），不再是一块灰底进度条。网页端默认收起这些，不占地方；终端和 QQ 仍按你设的工具详略档位。

- 第一次打开 Miyu 会先走一遍新手引导（`miyu oobe` 随时能再跑）：五屏，人格（用内置的 Miyu，或者起个名字、写几句设定捏一个）→ 功能（记忆、知识库、MCP、技能常开；闹钟、汇率、Arch、额度、表情包、生图、记账、语音，以及每个内置脚本和技能逐个勾）→ 认识你 → 终端集成（探到哪个 shell 就列哪个，装过的标「已集成」）→ 接模型。接模型可以借本机已登录的 claude / codex / antigravity，也可以从 16 条预设里挑（OpenAI、Anthropic、DeepSeek、OpenRouter、硅基流动、MiniMax、Kimi、智谱、MiMo、Gemini、opencode Zen/Go，国内海外站分开列）只填一个 key，opencode Zen 还能直接用公共密钥的免费额度；模型列表按 `/` 能搜。每屏「下一步」即落盘，中途退出不留半截配置。老用户升上来不会被拦。`miyu init` 仍是脚本化初始化，不会替你把引导标成做过。
- 空会话有了「大厅」：星空、渐变的 MIYU 艺术字、收窄的输入框嵌在字下面，模式行 `◉ 普通模式 ○ 开发模式 Tab 切换`。会话还是空的时候按 Tab 在普通 / 开发之间换车道，第一句话发出去就钉死。`/reset`、`/new` 回到大厅不再淡入，而且画布跟着清空，下一句话从屏幕顶上开始。艺术字可以用 `config/banner.txt` 换成自己的，`display.banner = false` 关掉；`miyu --banner` 只看这一幕。
- 全屏界面里正文改成顶部对齐：第一条消息在屏幕顶上，输入框钉在底部，中间允许留白。
- MCP 进了插件体系，和脚本一样受人格闸管：`persona.toml` 里 `[plugins]` 现在有 `mcp = [...]` 按服务器 id、`skills = [...]` 按技能名的白名单，关掉的服务器不会被拉起。dev 预设不带 MCP。
- 内置脚本和技能对自定义人格改成可选件：引导里逐个勾，勾了才挂；人格自己写的脚本、技能永远可用，不受白名单管。
- 删掉了 deep_research、诊断（check_issue）、输入法诊断技能和 `miyu normal`（裸 `miyu` 就是普通模式，`miyu dev` 保留）；AUR 那几个工具并进 archlinux 插件，工具名不变。

## 修复
- 在群里被禁言、之后又被解除禁言时，她不会再继续装死。她会先确认自己在这个群还能不能说话，而这个结论以前最长可以一直信到原定的解禁时刻——12 小时的禁言中途被提前解除，她照样闭嘴到第二天早上（实测撞上过两小时二十分钟：群里怎么喊、@她都没有一点反应）。现在这个结论最多只信 5 分钟，到点重新确认一次，并且不再拿上游缓存里那份过期答案当真。
- 她因为「自认还在禁言中」而不开口时，日志里会留下一条记录。以前这条路径是完全静默的：消息收下、不判断、不回复、一个字都不写，从外面看和掉线一模一样，事后也查不出所以然。
- opencodego（OpenCode Go）这个供应商恢复可用。发往它的请求少了一个会话标识头，服务端直接回 400 拒收，配在这个供应商下的模型一个都调不动。
- 网页端成员可以改自己的模型「思考程度（effort）」了，不再是管理员专属。以前成员在模型菜单里调档位会被后端以「admin only」拒掉，那条报错还会卡在页面顶上不消失。现在每个成员各存各的档位（落在自己家目录里），改了只影响自己这一轮往后的对话，既不动管理员的全局设置、也不影响别的成员；成员没设的模型走模型默认档。
- 中断（停止生成）现在是秒停,不再「点了灰一下、感觉加载很久才停」。真正的停止一直很快(后端从收到中断到落定约 12 毫秒),慢的是停下那一刻网页把整条对话全量重渲染了一遍——长对话尤其明显。现在中断只在原位补一条「本轮已中断」,不再整段重刷。
- 开启右侧 artifact 预览面板后,输入框页脚被面板盖住、点不到的问题修了(浮层态下面板底部会停在输入框上方)。
- 被问到问题时,输入框页脚不再重复显示竖排的「等待回答」——问题卡本身已经写着了。
- 刷新页面后,已回答的问答卡不再跳到回复正文的前面。它现在落在当初提问的位置(她说了半句话 → 问你 → 你回答 → 她接着说,这个顺序刷新后也保持)。
- 网页端「已回答」问答卡的问题与答案列表改为左对齐,不再缩进一截和标题错开。
- 新增 `/sandbox <路径>`：把当前会话关进一个目录。绑定之后她做的一切都只能在这个目录里进行——跑命令、后台任务、脚本、读写文件，连 Claude Code / Codex 这类 CLI 供应商的整个进程都一起关进去；家目录里别的东西（`~/.ssh`、`~/.config`、Miyu 自己的配置与数据库）读都读不到，只放行跑程序必需的系统目录、`/tmp` 和她自己的产出目录。为了锁进项目后还能编译，设置里多了「沙盒额外可写 / 只读」两份清单，默认放行 `~/.cargo` `~/.npm`（可写）和 `~/.rustup` `~/.local` `~/.gitconfig`（只读），并把 `~/.cargo/bin` `~/.local/bin` 补进 PATH。`/sandbox` 不带参数查看根和放行清单，`/sandbox clear` 解绑；终端、WebUI 输入框和 `miyu session sandbox` 三处都能用。绑定时就探测内核，没有 Landlock（Linux 5.13 以下、或 macOS）会当场拒绝而不是跑起来才失败。她能从系统提示词里看到自己关在哪、还能碰什么，所以读不到 `~/.ssh` 时会直接告诉你而不是瞎猜。刻意的代价：沙盒会话里 `git push` 走 SSH 会失败，要推就换个没绑沙盒的会话。绑定和解绑各会让缓存前缀失效一次。
- `/workspace` 命令退役。它只是设个工作目录、不锁任何东西，而工作目录现在由 `/sandbox` 的根决定，不带锁的工作区没有存在的必要。会话表里以前设过的工作目录升级时会被清空一次，不会有谁的老会话被暗中锁上；`miyu session workspace` 改名为 `miyu session sandbox`，`session show --json` 里的 `workspace` 字段改名为 `sandbox`。
- 终端里的图片不再时有时无。以前聊上几轮之后，图就开始整张整张地消失——什么都不显示，只留一片空白；重开一个终端又好了，过一会儿再犯。真正的原因是：图放不进屏幕剩下的那点空间时，终端得一边画一边往上滚，而 Konsole 这类终端在滚动途中会把整张图丢掉。而输入框一直占着屏幕底部，聊几轮光标就贴着底了，从那以后每一张图都放不下。现在打图之前先把地方腾出来，图完整画完再说。
- 从 kitty 里打开的其他终端（Ptyxis、GNOME 终端、foot 之类），图片也能正常显示了。kitty 会给子进程留下几个环境变量，从它里面开出来的终端会一路继承下去，负责画图的 chafa 就误以为自己还在 kitty 里，发出对方根本不认的图形协议——于是图一张都看不到。现在以终端自己报的身份为准，不再听上一层留下的话。
- 小图不再被放大成糊块。一张 128×128 的表情包以前会被撑到 540×540、平白占掉屏幕近半，现在按原大小显示（大图照旧会缩到合适尺寸）。
- Debian、Ubuntu、Fedora 41 及以下、openSUSE Leap、Alpine 这些发行版上，终端图片从「一张都出不来」变成正常显示。它们仓库里的 chafa 版本较旧，而 Miyu 之前用了只有 2026 年 2 月之后的 chafa 才认识的选项，对方不认就直接报错退出，任何图片、表情包、生成的图都显示不了，只留下一句 `chafa exited with status 2`。现在会先认一下 chafa 的版本，只传它认识的选项；chafa 没装时也直说「没装 chafa」，而不是丢一句看不懂的报错。
- Ctrl+C 打断正在跑的命令时，全屏画面里不再漏出旧样式的命令卡片，那一步收成红色「已中断」。
- 重开终端时被打断的那一轮也会回放出来（去掉尾巴上给模型看的提示，标一行「已中断」）；重开时的 markdown 表格不再比屏幕宽一格、边框折到下一行。
- shellhook 里回合失败（比如所有模型端点都不可用）现在会把错误打出来，以前那句被 shell 钩子吞掉了。
- `/reset` 在全屏里会顺手把画面清空（往回翻还在）。
- 终端里子代理的浮层有转轮了：正在跑、正在准备、正在想的那一行左边距上转着点阵，和主线一个样子；跑完就停。
- 子代理浮层里「Worked for …」点开之后是一条时间线：抬头底下接连线，收起来的每一步和抬头同一列，和主线那份一个样子，不再往右缩进一截。
- 子代理浮层里没有专门摘要规则的工具（比如 AUR 查询），那一行的窥视不再是一团 JSON，改成把参数的值按次序串起来（`info · zzq`）。
- 几十毫秒就跑完的步不再标 `0.0s`；交到后台的子代理那一行也不再挂一个没意义的秒数。
- 子代理浮层里「准备执行 · 0.0s」的秒数会走了，标题上的耗时也一起走；命令真正跑起来之后那一行会换成「运行命令 · 运行中」，不再一直挂着「准备执行」。终端里主对话派出的子代理以前从来收不到「命令开始跑」这条事件，浮层只能在准备态和结果之间跳。
- 子代理浮层里的「Worked for …」收缩行合着时是 `›`、点开是 `⌄`，和主线一样；以前合着开着都是 `⌄`。
- 后台子代理浮层里每一步尾巴上不再拼 ` · ok`；命令那一步点开是命令本身、空一行、输出，不再把抬头再说一遍。
- 全屏终端里命令跑着的时候底下露六行实时输出（原来四行），跑完之后这几行留在原地不收走，点开才是全部输出；收进「Worked for」之后点开也还在。
- 全屏终端里的链接是真的链接了：悬停有下划线，点一下直接打开（markdown 链接的标题、裸网址都行）。以前画屏那层把链接信息丢了，看着是普通文字。
- shellhook 里后台任务完成后的跟进回复，长得和你自己触发的那一轮一样了：暗色的「⚙ 后台任务跟进 · 任务名」抬头、时间线里的思考和工具步、正文。以前是另一套手搓的行渲染。顺手修了一个排队跟进第二轮时上一轮正文最后半行会粘到转轮那一行后面的老问题。
- shellhook 最开始的加载转轮和进时间线之后的转轮现在在同一列。
- 子代理浮层里它说的正文过 markdown 了：加粗、代码、列表都按样式显示，不再裸露星号和反引号。
- 配置文件被更新版本的 Miyu 改写过（版本号更高、多了新字段）之后，旧版本也能照常读它：不再拒绝启动，不会把版本号改回去，不认识的字段原样保留、写回时还在。
- 子代理浮层里正文的代码块、表格、公式按浮层宽度排版，不再出现大片空行和断成两截的行；后台子代理日志里一段话中的换行（标题、表格行、列表项）不再被丢掉。
- 后台子代理浮层里的转轮转得顺了（按时间定帧，开着浮层时刷新更快）；日志飞快增长时不再每帧重排一遍。
- 「防失忆提醒」和「每几轮发一次」两项从「自定义提示词」菜单挪到了「普通模式」菜单里。
- Arch 相关工具（AUR 查询、官方包查询、ArchWiki、Arch 新闻、装包、审包）在时间线上统一用 Arch 的图标。
- 「准备编辑」「准备执行」这类准备行的图标改成跟对应工具一样（铅笔、`$`），不再是通用齿轮；主线、子代理浮层、后台任务浮层都是。
- 主线「准备编辑 / 准备执行」时的转轮不再飞快乱抖（参数每流一片都重起一次转轮的老问题）。
- 全屏终端里 `/new` 会清空画布切到空会话，`/session` 切回去时回放那个会话最近几轮的对话。
- 全屏终端里等 `/compact` 这类命令时，鼠标一动屏上不再冒出一大段 `^[[<35;…M`（鼠标上报被当输入回显了）。
- 全屏终端里压缩上下文有输出了：正文里一行「正在压缩上下文…」，压完一行「上下文已压缩 · 用量」，点开能看压缩出来的摘要；回合里自动压缩也是这个样子，不再只有右上角一条通知。
- 后台任务状态行的转轮在 AI 流式输出时也转得顺了。
- 脚本接口：模型 `register` 完自己的脚本后调不到、`list` 也不列的问题修了。根因是人格勾过内置脚本之后白名单变成明细，新注册的脚本被裁掉；现在人格自己那一层不受白名单管。同时 `manage_script list` 看到的和真挂上的一致，未启用的脚本不再出现在模型面前。
- 纯中文的人格名以前会挤进同一个目录（`personas/md`），互相覆盖；现在按名字哈希分目录，老目录首次加载时自动搬走。
- 从全屏界面进设置（`/config`）再出来不再闪一下 shell 画面，光标也不再先跳到左上角再跳回输入框；引导结束进会话同样是平滑过渡。
- 空会话里切换模式不再弹「已切换到会话」。

## 本次发布流程修正

- Linux 容器真实安装与指定供应商模型输出验证。
- Rust 1.89 原子计数 API 兼容修复；Arch Python/bzip2 依赖和 Fedora 共享系统目录权限修复。
- 发布资产精确白名单、冻结输入与真实构建记录绑定、上传下载哈希回读、失败环境清理检查。

## 0.6.0 多发行版发布

- 新增 Linux x86_64 的 DEB、Fedora RPM 和 GNU tar；保留 Arch 主包与 voice。资源随包分发，包含三套字体、模型、表情、脚本、默认知识库和许可证。
- 构建输入、源码快照和资源固定哈希；断网构建并逐发行版安装，通过真实 opencodego/deepseek-v4.1-flash 对话后才发布。
- 修复安装前缀资源定位、可移动 tar 的私有 ONNX Runtime 查找；沙盒拆分为 Linux backend 与明确失败关闭的 unsupported backend。
- release note 附真实 OOBE 截图。0.6.0 本次发行包只覆盖已验收的 Linux x86_64，Mac 尚未验证，不提供正式资产。

- 发布兼容性检查：修正三处原子计数API，使已声明的 Rust 1.89 能实际检查通过；补齐 Arch 内置脚本/voice 的 Python、bzip2 依赖，并修复 Fedora 系统目录权限冲突。

## 0.6.0 包修订 2

- 裸 `miyu` 默认进入全屏 TUI，不再要求设置 `MIYU_TUI=1`。保留 `MIYU_TUI=0` 回退入口。
- Release 附件仅保留 Arch、Debian/Ubuntu、Fedora 的主包和语音包，截图引用仓库，SHA256 放在正文折叠区。
