# 选中文字右键菜单:解释 · 翻译 · 引用追问 · 搜索

> **2026-09-14 已归档**:步 1–4 落地(用户确认)。
> 总览:`../../plan/2026-09-14/main-tasks.md`。来源:2026-09-14 用户追加。遵循 `docs/理念.md`。
> 未跑测试,下文是读码推理;`未实测` / `未核实` 处施工时补数字。

## 0. 用户裁定(2026-09-14)

| 问题 | 裁定 |
|---|---|
| 触发 | 鼠标选中一段文字后右键,**拦截原生菜单**换成自己的 |
| 解释用哪个模型 | **设置 → 模型池 → 旁路请求**里选 |
| 解释要带什么上下文 | 这段话所在回合里**用户问了什么、她答了什么** |
| 菜单项 | **解释 · 翻译 · 引用追问 · 搜索** |
| 结果怎么显示 | **选区旁浮窗**(不进对话上下文) |

## 1. 现状(读码)

| 事实 | 位置 |
|---|---|
| WebUI 里没有任何 `contextmenu` 处理 | `web/*.js` |
| 旁路请求是 `AuxRole` 枚举,现有三种:会话标题 / 日记整理 / 深度研究 | `src/config/provider.rs:163` |
| 设置页「旁路请求」卡片按同一张表渲染,加一行即可 | `web/settings.js:1780` |
| 旁路请求的现成写法:`from_aux_role` + `with_request_scope("…")` 独立记账 + `chat_stream` | `src/web/sessions.rs:824` |
| 知识库搜索已有 HTTP 接口 | `src/web/server.rs:463`(`/api/dash/kb/search`) |
| 网页搜索只作为工具存在(`web_search`),**没有**给 WebUI 直接调的接口 | `src/tools/descriptions/web_search.json` |
| markdown 渲染是纯 DOM,模型文字不会变成标记 | `web/highlight.js` 头注释 |

## 2. 设计

### A. 触发与退路

- **只在聊天消息正文**(`.markdown-body`)里、且选区非空时拦截;其他地方、没选中文字时一律原生菜单。
- **Shift + 右键 = 原生菜单**,菜单底部写一行提示。拦截原生菜单不能把用户的退路也拿走。
- 我们的菜单**必须自带「复制」**——原生菜单被替掉之后,最常用的就是它。
- 键盘:`Esc` 关闭,方向键移动,`Enter` 执行。
- 手机:iOS 的长按菜单拦不住,也不该拦。选区稳定后(`selectionchange` 停 300ms)浮一条小工具条,项目同上。施工时放在选区下方,原因见 §6。

### B. 四个菜单项

| 项 | 做什么 | 走不走模型 |
|---|---|---|
| **解释** | 结合本回合上下文解释选中内容 | 旁路请求 |
| **翻译** | CJK 占多数译成英文,否则译成中文;浮窗里可切换目标语言 | 旁路请求 |
| **引用追问** | 把选中文字以 `> 引用` 插进输入框并聚焦,用户接着打字 | 不走,纯前端 |
| **搜索** | 用选中文字搜索,结果列表进浮窗 | 知识库走现有接口,网页走新接口(§4 裁定:两栏都列) |

### C. 解释 / 翻译接口

`POST /api/selection/assist {session_id, turn_id, action: "explain" | "translate", text, target_lang?}` → 流式返回。

- **上下文由后端按 `turn_id` 从库里取**,不收前端传来的「用户问了什么 / 她答了什么」:
  - 用户侧取 `raw_content`,她那侧取展示内容——正是理念「三份内容分离」里给人读的那两份,不带工程附加物。
  - 前端传上下文的话,等于任何人都能拼一段「她说过的话」喂给模型。
- **会话归属校验**:成员只能对自己的会话发起。
- 长度上限:选区 ≤ 2k 字;上下文两侧各 ≤ 8k 字,超了保留头尾。数字施工时按旁路档位的窗口定。
- 新增 `AuxRole::SelectionAssist`(配置键 `selection_assist`),缺省档位 `lite`;设置页「旁路请求」卡片多一行「划词解释 / 翻译」。
- `with_request_scope("selection-assist")`:独立记账,不算进主对话缓存(AGENTS.md §1.7)。
- 提示词是**常量英文短句**(AGENTS.md §1.5),选区与上下文装进 XML 标签外壳;输出语言跟随用户。
- 浮窗关闭即中断流,不在后台白跑。

### D. 浮窗

- 锚在选区矩形旁,放不下自动翻到另一侧;手机上变底部弹出面板。
- 流式出字,内容走现有纯 DOM `renderMarkdown`,不开 HTML。
- 操作:**复制 · 重试 · 钉住 · 转成追问**(把「引用 + 解释」一起塞进输入框)。
- 钉住后可以再选别的词开第二个浮窗;未钉住的点外部即关。
- 按住标题栏可拖动,拖过即自动钉住(2026-09-14 看预览后追加,见 §6)。
- **不进对话、不进记忆和日记、不落库。**

### E. 搜索

- 知识库:复用 `/api/dash/kb/search`。已核实(见 §6):按账号取各自的知识库,成员只搜得到自己的。
- 网页:没有现成 HTTP 接口,要新开一个,复用 `web_search` 工具背后的同一个搜索实现,不另写抓取。
- 结果列表进浮窗:网页结果点开新标签;知识库结果点开进侧栏预览(与 `../../plan/2026-09-14/file-links.md` 的侧栏临时视图同一槽位)。

## 3. 理念对照

| 理念 | 落到这里 |
|---|---|
| 前缀即契约 · 三份内容分离 | 解释结果不进主对话上下文,主对话前缀一个字节不动;旁路请求独立 scope |
| 权限由代码承担 | 上下文后端取、会话归属后端判;前端没有能力伪造「她说过的话」 |
| 文风规范 | 旁路提示词常量英文,不拼时间戳;选区与上下文是不可信文本,装在标签里 |
| 报错要说真原因 | 旁路档位没配模型时直接说「设置 → 模型池 → 旁路请求 → 划词解释 没有可用模型」,不说「请求失败」 |

## 4. 裁定(2026-09-14)

1. 解释用**中性说明**,不带人格提示词。
2. 搜索**知识库与网页两栏都列**,知识库在前。
3. 结果**不留历史**。
4. 侧栏里的选区**先不做**。

## 5. 施工拆分

| 步 | 内容 | 动的地方 | 验证 |
|---|---|---|---|
| 1 | 菜单壳 + 复制 + 引用追问 + Shift 退路 | `web/`(独立文件,不再往 `app.js` 里塞) | 选区外 / 无选区 / Shift 三种都走原生菜单 |
| 2 | `SelectionAssist` 档位 + 设置项 + 接口 + 解释 / 翻译浮窗 | `src/config/provider.rs`、`src/web/`、`web/` | 成员对别人会话发起返回 403;关浮窗后服务端流确实中断 |
| 3 | 搜索 | `src/web/`、`web/` | 知识库、网页两栏各自出结果或出错;成员网页一栏 403 |
| 4 | 手机浮动工具条 | `web/` | iOS Safari、Android Chrome 真机 |

日期见总览 §2:1–4 于 **2026-09-14** 施工(用户要求提前),2026-09-15 验收。

## 6. 施工记录(2026-09-14)

> 按用户要求**暂不编译、不跑测试**。只做了 `node --check`(`selectionmenu.js`、`app.js`、`settings.js` 语法通过)。
> Rust 部分没过 `cargo check`,验收前第一件事就是编译。

**做了什么**

| 部分 | 文件 |
|---|---|
| 旁路档位 `AuxRole::SelectionAssist`(键 `selection_assist`,缺省 lite) | `src/config/provider.rs`、`src/config_tui/tiers.rs`(TUI 标签)、`web/settings.js`(旁路请求卡片多一行) |
| 解释 / 翻译接口 `POST /api/selection/assist`,NDJSON 流(`delta` / `done` / `error`) | `src/web/selection_menu.rs` |
| 网页搜索接口 `GET /api/selection/web-search?q=`,复用 `web_search` 的实现 | `src/web/selection_menu.rs`、`src/tools/web/mod.rs`(`search_for_webui`)、`src/tools/mod.rs` |
| 路由与静态资源 | `src/web/server.rs`、`src/web/assets.rs`、`src/web/mod.rs`、`web/index.html` |
| 前端菜单、浮窗、搜索两栏、手机工具条 | `web/selectionmenu.js`、`web/app.js`(挂载)、`web/styles.css` |
| 文档 | `docs/wiki/03-使用方式.md` §4、`docs/wiki/05-配置指南.md`(旁路请求) |

**与 §2 设计不同的地方**

- 手机工具条放在选区**下方**:iOS 系统长按菜单在上方,放上面会互相盖住。
- 网页搜索**只给管理员**:查询词会发到外部搜索服务,成员的插件白名单另有口径,先收紧。
- 知识库结果点一下**先复制路径**:侧栏临时视图(`file-links` 步 2)还没建,建好后改成侧栏预览。
- §2.E 的 `未核实` 已核实:`/api/dash/kb/search` 用 `require_identity`,按账号取各自的知识库,成员只搜得到自己的。
- 模型调用放在独立线程的单线程运行时里跑,流式增量经 channel 写进响应体;浏览器关浮窗时接收端关闭、回调发送失败即中止模型请求。原因写在 `selection_menu.rs` 头注释。
- 不可信文本里的 `</` 用零宽连接符拆开,防止提前闭合 `<selection>` / `<conversation>` 外壳。
- 浮窗**可拖动**(2026-09-14 看预览后用户要求):按住标题栏拖,标题栏始终留在视口内;拖过即自动钉住;手机底部面板不拖。
- 浮窗**文字溢出**修复(2026-09-14 用户报):根因是浮窗为 grid、隐式列宽 auto,一段不可断的长串(URL、路径、长英文词、报错原文)把列撑得比浮窗宽。列钉成 `minmax(0, 1fr)`、子项 `min-width: 0`,正文 / 提示 / 摘要允许任意处折行,代码块改块内横向滚动。只改 `web/styles.css`。

**`未核实`,编译或验收时注意**

- `OpenAiCompatibleClient` 与 `StateStore` 能否 move 进 `std::thread::spawn`(需要 `Send`)。编不过就改成在 daemon 的本地任务里跑、用 channel 回传。
- `ApiError::internal` 是否接受 `JoinError`。
- `web_search` 的返回文本格式:前端能解析出 `results` 数组就画列表,否则整段按 markdown 显示。
- 知识库结果条目的字段名:前端按 `path / rel_path / file / name / title` 依次取,取不到显示「(无路径)」。

**验收流程(2026-09-15)**

1. `cargo check --all-targets` 通过后重新构建、重启 daemon。
2. 设置 → 模型池 → 旁路请求:出现「划词解释 / 翻译」一行,缺省是 lite。
3. 在她的一条回复里选中一个词,右键:出现「解释 · 翻译 · 引用追问 · 搜索 · 复制」;按住 Shift 右键出现浏览器原生菜单;在输入框、侧边栏里右键仍是原生菜单。
4. 解释:浮窗贴在选区旁,流式出字,口吻中性;点「转成追问」后输入框里是引用 + 解释。
5. 翻译:中文选区译成英文、英文选区译成中文;浮窗里「改译英文 / 改译中文」能切。
6. 解释进行中关掉浮窗:daemon 日志里这次旁路请求应中止,不继续出字。
7. 引用追问:输入框里出现 `> 选中文字`,光标在末尾。
8. 搜索:知识库一栏、网页一栏各自出结果或出错;成员账号的网页一栏显示无权限。
9. 钉住一个浮窗后再选别的词解释:两个浮窗并存;没钉住的点别处就关。
10. 手机宽度:浮窗是底部面板;真机上选中文字后下方出现工具条。
11. 旁路档位没配模型时:浮窗里显示「设置 → 模型池 → 旁路请求 → 划词解释 没有可用模型:…」。
