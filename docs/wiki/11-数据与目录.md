# 11 · 数据与目录

> ⚠️ **临时版本提示**：本文档由 DeepSeek 根据当前 Miyu 源码逐行整理，属于临时参考版本；正式版文档仍在施工中，内容可能与最新代码存在出入。请以 `miyu -h`、`miyu config`、仓库 README 及源码为准。

## 1. 根目录：`~/.miyu`

默认一切都在 `~/.miyu`（`MIYU_HOME` 可改）。目录权限都是 0700（只有你自己能进）。

```text
~/.miyu/
├── .home-layout-v1               家目录布局标记,内容 = 管理员家目录名
├── config/                       系统配置（/etc）
│   ├── config.jsonc              主配置（JSONC，允许注释）
│   ├── dev-prompt.md             开发模式提示词
│   └── shell/                    bash/zsh hook 文件
├── personas/                     管理员发布的共享人格（原 data/personas）
│   ├── default/                  memory/（记忆库）、meme.db、persona.toml（启用集）
│   └── dev/
├── extensions/                   已装扩展（原 data/skills、data/scripts）
│   ├── skills/
│   └── scripts/
├── home/<用户名>/                人的东西（第一个账号 = 管理员，通常 home/<系统用户名>/）
│   ├── conversation.db           会话/回合（WAL）；管理员这份还存账号/邀请码/平台绑定，成员的只有自己的会话
│   ├── profile.md                「希望 AI 如何认知你」（原 identities/user-identity.md；通讯平台不看）
│   ├── identities/               人格专属的用户身份 .md
│   ├── settings.json             成员偏好：当前人格、引导是否做过
│   ├── kb/                       成员自己的知识库（勾了知识库插件才用到）
│   └── personas/<id>/            成员自建人格：persona.md / persona.json / persona.toml / avatar.* / board.* / memory/ / skills/ / scripts/
│   ├── artifacts/<session_id>/   WebUI Artifact
│   ├── documents/                写出的报告与文档
│   ├── pictures/                 web-images/ 搜图、generated-images/ 生图
│   ├── ledger/ledger.db          记账账本（独立库）
│   └── shares/<share_id>/        WebUI 分享文件托管区（原 data/shared）
├── data/                         其余系统数据
│   ├── prompts/                  人格 .md、system-prompt.md
│   ├── persona-avatars/          人格头像
│   ├── memes/                    表情库
│   ├── kb/                       知识库
│   └── platforms/onebot/message_history/history.sqlite3  群聊消息历史
├── cache/
│   ├── logs/                     miyu.*.log、cache-usage.*.jsonl、requests-*.jsonl、alarm.log
│   ├── jobs/<id>.log             后台任务日志
│   ├── clipboard_images/         剪贴板图片
│   ├── platform_files/qq/        QQ 文件下载
│   └── aur-review/ aur-install/  AUR 审查/安装缓存
└── state/                        机器运行的东西（/var）
    ├── attachments/<att_id>/     WebUI 上传的附件本体
    ├── usage.json                累计用量
    ├── usage-history.jsonl       逐条用量明细（每行带 acct = 账号）
    ├── thinking-variants.json    思考档位选择
    ├── daemon-launch.json        daemon 启动配置（不含明文密码）
    ├── web-passwords/            Web 密码（0600）
    ├── repl-history.jsonl        REPL 输入历史
    ├── alarms.json               闹钟共享状态
    ├── todos/<session>.json      Todo
    ├── skill-drafts/             skill 草稿
    ├── spill/                    超长工具输出落盘
    ├── persona-hints/            人格提醒缓存
    ├── personas/<p>/memory/      逐出上下文库
    ├── prompt-fingerprints/      提示词 SHA-256 指纹
    ├── arch_news_last_seen.json  Arch 新闻已读游标
    └── aur-review-state.json     AUR 审查状态
```

实际路径可用 `miyu paths` 打印，`miyu layout` 看布局与搬家计划。没有 `.home-layout-v1` 标记的是老布局：会话库在 `state/`，人格/技能/脚本/身份/图片/文档/账本/分享都在 `data/` 下；升级后 daemon 首次启动自动搬（见 [12-备份迁移与更新](12-备份迁移与更新.md)）。

## 2. 系统级目录（打包安装）

| 路径 | 内容 | 缺失时的表现 |
|---|---|---|
| `/usr/share/miyu/fonts/` | Noto CJK + Color Emoji | QQ 长回复转图退化为纯文本 |
| `/usr/share/miyu/memes/miyu/` | 内置表情 | 默认人格无内置表情 |
| `/usr/share/miyu/default-kb/` | 默认知识库 | 默认知识库为空 |
| `/usr/share/miyu/scripts/` | 系统脚本 | 无内置脚本 |

## 3. 数据库要点

- 主库 `conversation.db` 使用 WAL、busy_timeout 5s、外键开启；迁移 24 个版本，每个迁移在 immediate 事务里做并跑 FK check。
- 账本 `data/ledger/ledger.db` 是独立库，不挂主库：主库每轮对话都在写，账本一天几笔，而 SQLite 损坏几乎都发生在写入过程中——写得越少越安全，主库真出事时账本也不陪葬。它用 `synchronous=FULL`（吞吐无所谓，断电不丢刚记的账），打开时跑 `quick_check`，迁移前用 `VACUUM INTO` 备份成 `ledger.db.bak`。
- **不要**在 daemon 运行时直接删/改 DB；先 `miyu daemon stop`。
- 旧版本迁移到 `~/.miyu` 时：旧 XDG 目录里的 config/data/cache/state 会被搬过来；`Documents/Miyu` → `data/documents`；`Pictures/miyu`、`Pictures/Miyu` → `data/pictures`。
- 迁移前会两遍预检，失败零写入；符号链接、跨文件系统等危险情况会拒绝或丢弃缓存。

## 4. 运行期目录

- IPC socket：`$XDG_RUNTIME_DIR/miyu/core.sock`；没有 XDG_RUNTIME_DIR 时回退 `state/miyu`。
- 锁文件：`core.lock`（daemon/web 单实例）、`starter.lock`（启动互斥）。
- 权限：socket 目录 0700、密码文件 0600。

## 5. 日志

| 文件/流 | 内容 |
|---|---|
| `cache/logs/miyu.*.log` | 按天滚动 8 份；默认 ERROR，QQ 日志 INFO |
| `cache/logs/daemon.log` | daemon 启动 banner/panic 等非结构化输出 |
| `cache/logs/cache-usage.<date>.jsonl` | 每请求 token 绝对值（默认开，保留 14 天） |
| `cache/logs/requests-<date>.jsonl` | 完整请求体（默认关，`MIYU_LOG_REQUESTS=1`） |
| `cache/logs/alarm.log` | 闹钟 worker 日志 |

`--debug` 把主日志提到 DEBUG。`daemon logs request` 是进程内存开关，不落配置。
